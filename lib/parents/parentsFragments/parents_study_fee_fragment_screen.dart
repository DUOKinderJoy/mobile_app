import 'package:flutter/material.dart';

import '../../meal_selection/meal_home_page.dart';

class ParentsStudyFeeFragementScreen extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text(
            "Study Fee Fragement Screen",
          ),
        )

    );
  }
}
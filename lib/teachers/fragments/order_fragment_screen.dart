import 'package:flutter/material.dart';

class OrderFragementScreen extends StatelessWidget
{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
          child: Text(
            "Order Fragement Screen",
          ),
        )
    );
  }
}